# 모든지 강의 문서 편찬 규격 (서브에이전트용)

이 문서는 `vault/{섹션}/` 강의 문서를 작성하는 에이전트가 반드시 따라야 하는 규격이다.
작성 전에 반드시 기존 모범 문서 한 편을 읽어라: `vault/philosophy/06-경험론-3강-흄-인과의-해체.md`
(철학 문서지만 구조·문체·문제 품질의 기준은 모든 섹션에 동일하게 적용된다.)

## 파일 규격

- 경로: `vault/{섹션}/{과목번호2자리}-{과목명}-{강번호}강-{제목-하이픈-연결}.md`
- 과목명은 하이픈 없는 한 덩어리로 쓴다 (예: 수학의언어, 대수학). 빌드가 두 번째 하이픈까지를 과목 그룹으로 파싱한다.
- 문서 구조 (순서 고정):
  1. `# {과목명(띄어쓰기 허용)} {강번호}강. {제목}` (H1 하나)
  2. `> 한 줄 핵심 명제` (인용 블록, 강 전체를 요약하는 도발적 한 문장)
  3. `## 풀고 시작` + quiz 블록 1문제 (배우기 전에 찍는 사전시험. 본문의 핵심 개념을 겨냥)
  4. 본문 섹션 3~5개 (`##`). 개념 전개 + 구체 사례 + 역사적 맥락(인물·일화) 1개 이상 + 후속 강/과목과의 연결 언급
  5. `## 인출 문제` + quiz 블록 4문제 (본문을 덮고 푸는 문제)
  6. (선택) `## 생각해볼 질문 (정답 없음)` 1~2개
  7. `---` 뒤에 이전/다음 위키링크 한 줄

## quiz 블록 문법 (정확히 지킬 것, 빌드 파서가 읽는다)

````
```quiz
Q: 질문 한 문장?
- 오답 보기
-* 정답 보기
- 오답 보기
- 오답 보기
E: 해설 2~3문장. 왜 정답인지 + 오답이 왜 그럴듯하지만 틀렸는지.
```
````

- 보기는 반드시 4개, 정답은 정확히 1개(`-*` 접두).
- 오답은 그럴듯해야 한다(과잉교정 효과 유도). 뻔한 오답 금지.
- 인출 문제 4개는 본문의 서로 다른 섹션을 커버한다.
- Q/보기/E 안에서 백틱, 이중대괄호, 파이프 문자를 쓰지 마라.

## 수식·기호 규칙 (수학·과학 섹션)

- LaTeX·MathJax는 없다. 수식은 평문과 유니코드로 쓴다: x², √2, π, ≤, ∞, ℕ.
- 복잡한 유도 대신 개념과 논증을 우선한다. 꼭 필요한 짧은 식은 본문에서 인라인 코드로 감쌀 수 있다 (quiz 블록 안에서는 백틱 금지이므로 평문으로).
- 분수는 "a/b", 지수는 유니코드 위첨자나 "x^n" 표기를 쓴다.

## 문체 규칙

- 한국어 문어체 "~다"로 끝나는 완결형 문장. 강의하듯 명료하게.
- 엠대쉬(—) 절대 금지. 마침표·쉼표·콜론·괄호로 대체.
- 전문용어 첫 등장 시 한글(원어) 병기. 예: 가추(abduction).
- 표(마크다운 테이블)는 비교·대조에 적극 사용.
- **굵게**는 핵심 개념·명제에만. 남발 금지.
- 분량: 본문 900~1400자 어절 기준(공백 포함 대략 1800~2800자). 얕은 요약이 아니라 논증의 뼈대가 보이게.
- 지적 정직성: 통설과 논쟁점을 구분하고, 반론이 있으면 반드시 소개한다. 사실 관계(연도, 인명, 정리 이름)는 정확해야 한다. 확실치 않은 세부는 쓰지 마라.

## 위키링크

- 이전/다음: `[[{섹션}/{파일명(확장자 제외)}|표시명]]` 형식.
- 문서 내 다른 과목·섹션 언급 시에도 같은 형식으로 링크 가능(선택).
- 파일명의 파이프는 본문 표 안에서만 `\|`로 이스케이프.

## 금지

- 사실 날조, 존재하지 않는 인용문·일화. 인용은 널리 알려진 것만.
- 다른 과목의 파일 수정. 자기가 맡은 강 파일만 생성한다.
- index.md 수정 금지 (편찬 완료 후 조율자가 갱신한다).
